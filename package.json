{
  "name": "wwwd-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 8844",
    "build": "astro check && astro build",
    "verify": "node scripts/verify-site.mjs",
    "preview": "astro preview --host 127.0.0.1 --port 8844"
  },
  "dependencies": {
    "astro": "^7.2.6",
    "typescript": "latest"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "playwright": "^1.62.1"
  }
}
